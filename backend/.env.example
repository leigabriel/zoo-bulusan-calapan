# Server Configuration
PORT=5000
NODE_ENV=development

# Backend URL (for generating correct URLs in responses)
# For production: https://your-api.onrender.com
# BACKEND_URL=https://your-api.onrender.com

# Database Configuration
# For local development
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_database_password
DB_NAME=zoo_bulusan

# For production (e.g., PlanetScale, Railway MySQL, Aiven)
# DB_HOST=your-db-host.provider.com
# DB_PORT=3306
# DB_USER=your_prod_user
# DB_PASSWORD=your_prod_password
# DB_NAME=your_prod_database
# DB_SSL_REJECT_UNAUTHORIZED=false  # Set to false for Aiven (self-signed certs)

# JWT Configuration
# Generate a secure random string: openssl rand -base64 32
JWT_SECRET=your-secure-random-secret-key-min-32-chars
JWT_EXPIRE=7d

# CORS Configuration
# For production, use your Vercel domain
CORS_ORIGIN=http://localhost:5173

# Frontend URL (for OAuth redirects)
# For production: https://your-app.vercel.app
FRONTEND_URL=http://localhost:5173

# Google OAuth 2.0 Configuration
# Get these from Google Cloud Console: https://console.cloud.google.com/apis/credentials
# See docs/GOOGLE_AUTH_SETUP.md for detailed setup instructions
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
# For production: https://your-api.onrender.com/auth/google/callback
GOOGLE_CALLBACK_URL=http://localhost:5000/auth/google/callback

# Gemini API Key (for AI features)
GEMINI_API_KEY=your-gemini-api-key
